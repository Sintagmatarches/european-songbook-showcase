{
  "name": "european-songbook-portfolio",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/apply-portfolio-ui-policy.mjs && node scripts/build-static-demo.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build",
    "snapshot:catalog": "node scripts/snapshot-public-catalog.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
